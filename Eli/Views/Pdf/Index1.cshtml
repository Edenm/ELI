@model IEnumerable<eli.models.tblPatient>

    @{
        ViewBag.Title = "PDF TRY";
    }

    <h2>PDF TRY</h2>
    @{Layout = "~/Views/Shared/_LoginLayout.cshtml";}



    <h2>PDF TRY</h2>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>


            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>

            </tr>
        }

    </table>
