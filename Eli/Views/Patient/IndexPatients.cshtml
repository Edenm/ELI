@model IEnumerable<Eli.Models.tblPatient>
@using Eli.Models;
@using System;
@using System.Collections.Generic;

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title></title>
    <meta name="viewport" content="width=device-width" />
    <script src="~/Scripts/DataTable/dataTables.keyTable.js"></script>
    <script src="~/Scripts/DataTable/jquery.js"></script>

    <script src="~/Scripts/DataTable/jquery.dataTables.js"></script>
    <link href="~/Scripts/DataTable/dataTables.keyTable.css" rel="stylesheet" />
    <link href="~/Scripts/DataTable/jquery.dataTables.css" rel="stylesheet" />

    <meta name="viewport" content="width=device-width, initial-scale=1">
    @Styles.Render("~/Content/bootstrapcss")
    @Styles.Render("~/Content/themes/base/css")

</head>
<body >
    <h2 align="center" style="color:blue">מטופלים</h2>
    <h4 align="right">:חיפוש</h4>
    <div dir="rtl">
        <table id="myTable" class="display nowrap table-bordered" >
            <thead align="center">
                <tr>
                    <th>תעודת זהות</th>
                    <th>שם פרטי</th>
                    <th>שם משפחה</th>
                    <th>טלפון</th>
                    <th>פרטים נוספים</th>
                    <th>עריכה</th>
                    <th>הפניות</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr align="center">
                        <td> @item.ID </td>
                        <td> @item.PatientFirstName </td>
                        <td> @item.PatientSurName </td>
                        <td> @item.ContcatPhoneNumber </td>
                        <td>
                            @{
                                String id = "1" + @item.ID;
                            }
                            <a data-toggle="modal" data-target="#id">
                                <img src="~/Images/info.png" />
                            </a>
                        </td>
                        <td>
                            <a data-toggle="modal" data-target="#@item.ID">
                                <img src="~/Images/edit.png" />
                            </a>
                        </td>
                        <td>
                            <a href="@Url.Action("IndexReference", "Reference", new { pid = @item.ID },null)">
                                <img src="~/Images/info.png" />
                            </a>
                        </td>
                    </tr>
                    
                }
            </tbody>
        </table>
    </div>

    @foreach (tblPatient item in Model)
    {
                @Html.Partial("_Patient", item);
    }
    @foreach (tblPatient item in Model)
    {
                @Html.Action("_EditFamily", "Patient", new { id = item.ID });
    }

    <script>

        var table = $('#myTable').DataTable();
        new $.fn.dataTable.KeyTable(table);

    </script>

</body>
</html>

